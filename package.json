{
  "name": "atlas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "dev": "vite",
    "build": "cd backend && npm install && npx prisma generate && cd .. && vite build",
    "preview": "vite preview",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.14"
  }
}
